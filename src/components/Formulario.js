import React from 'react';

const Formulario = () => {
    return (
        <form>
            <div>
                <label>
                    <select>

                    </select>
                </label>
            </div>
        </form>
    );
}

export default Formulario;